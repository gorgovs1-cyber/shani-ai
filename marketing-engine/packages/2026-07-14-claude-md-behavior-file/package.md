נושא: קובץ CLAUDE.md אחד שמשנה איך Claude Code מתנהג — ומה שני עצמה כבר מריצה בפועל בפרויקטים שלה
הזדמנות מקור: OPP-20260714-2
תאריך יצירה: 2026-07-14
עמוד תוכן: 1 + 4 · סוג: חינוכי (עיקרי, תיקון תפיסה מוטעית + לימוד מנגנון אמיתי) / מאחורי-הקלעים (משני, ההוכחה מהעבודה של שני)

## החלטת פורמט
פורמט: ריל faceless באינסטגרם.
נימוק: תואם ישירות את נוסחת הריל ב-`patterns.md` (הוק 0–3ש' → הבטחה 3–6ש' → הדגמת מסך 6–25ש' →
CTA 25–35ש') ואת דפוס הוק #1 שם ("גילוי יכולת — Claude עכשיו יודע/יכול..."), שמתאים ישירות לנושא
(קובץ הנחיות שמשנה בפועל איך Claude Code מתנהג). זהה לפורמט הראשוני המוצע בהזדמנות — אין סטייה.
נבדקה חלופת קרוסלה ("N דברים" — מתאימה מבנית ל"4 הבעיות" שמוזכרות ברפרנס) ונפסלה כפורמט ראשי:
הליבה של הזווית היא **ראיה חיה** (הקובץ האמיתי של שני + עקרון-פעולה של תוסף פנימי) — זה דורש
הדגמת מסך אמיתית עם תזוזה וזמן, לא רשימת פריטים סטטית. מבנה "4 דברים" עדיין נשזר *בתוך* הריל
כדוגמאות מוצגות (לא רשימה סגורה/ממצה — לפי ההסתייגות בהזדמנות), לא כקרוסלה נפרדת.
Faceless חובה: כל התסריט הוא הדגמת מסך אמיתית + טקסט-על-מסך + כתוביות, בלי שום סגמנט מול מצלמה.

## זווית
לא "יש טריק סודי אחד שגורם ל-Claude להיות חכם" (המסגור של הרפרנס — סקרנות ריקה בלי לחשוף כלום)
אלא "הנה מה שבאמת קורה כשנותנים ל-Claude Code קובץ הנחיות-פרויקט: זה מתועד רשמית ע"י Anthropic,
זה לא קסם (יש לו תקרה ידועה — לא 100% ציות), וככה זה נראה אצלי בפועל — כולל כלל מפורש שכתבתי
בקובץ עצמו נגד בנייה מוגזמת (over-engineering)". תרגום לעסק (קהל לא-טכני,
content-strategy.md §8): קובץ הנחיות הוא כמו מסמך קליטה קבוע לעובד חדש — לא צריך להסביר לו
מחדש בכל שיחה מי אתה, מה העסק שלך ואיך אתה רוצה לעבוד. ההבחנה המרכזית מהרפרנס: תוכן-סמכות
(מתעד + מאמת + מראה בפועל) מול תוכן-הוק ריק (מבטיח בלי לחשוף).

הערה מכוונת: העיקרון הכללי של "4 הבעיות" (בנייה מוגזמת, התעלמות מהוראות, טענת "סיימתי" מוטעית,
הזיית API) מוצג בריל **כדוגמאות נפוצות**, לא כרשימה ממצה או כ"ארבעת הדברים" של רפרנס כלשהו —
בהתאם למגבלה המפורשת בהזדמנות (הניסוח/המסגור הספציפי לא אומת עצמאית).

> **תיקון תוכן (סבב 3, לבקשת שני — ר' `content-desk-package.md` סעיף 5ב):** הגרסה הקודמת (סבב 2,
> שמורה בהיסטוריית git של הקובץ — לא מופיעה בהמשך המסמך הזה) סטתה מהמסר האמיתי של הרפרנס — הפכה
> מ"קובץ שעוצר בעיות התנהגות קונקרטיות ומוכיח כל אחת בהדגמת מסך" ל"קובץ שמזכיר לקלוד מי אני".
> תוקן במלואו לגרסה הבאה: 7 שוטים, 30 שניות, 3 כללים קונקרטיים ומוכחים ממקור אמיתי (CLAUDE.md),
> פנייה ניטרלית-מגדרית לכל הקהלים (לא "רוצות"/"בעלות עסקים"/"לכן"). **אושר ע"י שני במלואו לפני
> התיקון** (כולל תיקון קריינות שוט 2).

## הוק
"קובץ אחד גרם לקלוד קוד להפסיק לעשות שלושה דברים שעיצבנו אותי בכל פרויקט." — טקסט על גבי הקלטת
מסך אמיתית של קובץ ה-CLAUDE.md האמיתי, נפתחת ישירות עליו.

## תסריט מלא (ריל) — קריינות (7 בייטים, 30 שניות)
1. (0–3ש') "קובץ אחד גרם לקלוד קוד להפסיק לעשות שלושה דברים שעיצבנו אותי בכל פרויקט."
2. (3–6ש') "קוראים לו CLAUDE.md, ובתוכו כתבתי כללים ברורים לאיך לעבוד איתי בפרויקט."
3. (6–11ש') "הראשון: לא לבנות מערכת שלמה כשביקשתי שינוי קטן."
4. (11–16ש') "השני: לא להמציא פרטים על העסק, המחירים או הלקוחות שלי."
5. (16–21ש') "השלישי: לא להסכים איתי אוטומטית. אם רעיון חלש, להגיד לי את זה."
6. (21–25ש') "במקום להסביר הכול מחדש בכל שיחה, הכללים כבר מחכים לו בתוך הפרויקט."
7. (25–30ש') "לקבלת רשימת הכללים המלאה, כתבו 'הנחיות'."

## טקסט על המסך (כיתוב מסונכרן, לפי שוט)
1. (0–3ש') "3 הרגלים שעצרתי עם קובץ אחד"
2. (3–6ש') "CLAUDE.md = כללי העבודה של הפרויקט"
3. (6–11ש') "1. בלי בנייה מוגזמת"
4. (11–16ש') "2. בלי להמציא מידע"
5. (16–21ש') "3. ביקורת אמיתית, לא ליטופים"
6. (21–25ש') "קובץ אחד. הקשר קבוע."
7. (25–30ש') כרטיס CTA+Outro משולב: טקסט ראשי "כתבו 'הנחיות'" · טקסט משני "לקבלת רשימת הכללים
   המלאה" · תחתית הכרטיס: @shani.creates.ai או SHANI-AI.COM (לפי המיתוג הפעיל)

## Caption
קלוד קוד היה עושה לי שוב ושוב את אותם הדברים:
בונה יותר ממה שביקשתי, משלים פרטים שהוא לא באמת יודע, ולפעמים מסכים איתי במקום לתת ביקורת אמיתית.
אז הכנסתי לפרויקט קובץ CLAUDE.md עם כללי עבודה ברורים.
הוא לא הופך את המודל למושלם, אבל הוא נותן לו הקשר קבוע ומגדיר איך לעבוד איתי בתוך הפרויקט.
לקבלת רשימת הכללים המלאה, כתבו "הנחיות".

## CTA
לקבלת רשימת הכללים המלאה, כתבו "הנחיות". (תגובה→DM, כרטיס CTA+Outro משולב — שוט 7)

## מילת מפתח ManyChat
"הנחיות" — הצעה בלבד, טרם שוריינה. נבדקה מול `keywords.md`: הקיימות שם (מייל, פרומפט, הצעה,
פרומפטים) כולן "⚠️ לא מאומת" ונספרות כתפוסות לצורך דה-דופ זהיר. "הנחיות" לא מופיעה בטבלה בשום
סטטוס — אין התנגשות. יש לשריין בפועל ב-`keywords.md` רק לפני פרסום.

## קונספט מדריך / ליד-מגנט
כותרת: מה שמים בקובץ הנחיות אחד שקלוד קוד קורא בכל שיחה
תוכן (5 נקודות):
1. מי אתה ומה העסק שלך — כדי שלא תסביר/י מחדש בכל שיחה.
2. איך אתה רוצה שהוא ידבר איתך — טון, כנות, בלי הסכמה אוטומטית.
3. מה מותר ומה אסור לו "להמציא" — מיתוג, לקוחות, תמחור, סטטוס מוצר.
4. עיקרון אחד נגד בנייה מוגזמת — "תעשה רק את המינימום הנדרש".
5. למה זה לא קסם — ולמה כדאי לבדוק תוצאה גם כשיש קובץ כזה.

## חיבור להצעה
`offers/ai-tools.md` — זווית "כלים פנימיים מותאמים": הקובץ + התוסף הפנימי הם דוגמה חיה לאותה
קטגוריית עבודה ששני בונה ללקוחות (דשבורדים/מערכות/כלים פנימיים מותאמים). אין CTA מכירתי ישיר
בחבילה זו — זה תוכן חינוכי (60%)/מאחורי-הקלעים (20%), לא קידום ישיר (10%). החיבור מוזכר בהקשר
בלבד בתוך ה-caption (משפט אחד), בלי הפניית מחיר. אם ירצו הרחבה — ההפניה התקנית היא `/audit` או
עמוד המחירים, לא מוזכרת כאן כי אין CTA מכירתי לחבילה זו.

## מקורות ועובדות (בירושה מ-OPP-20260714-2, ותוקן בסבב ביקורת זה — ר' להלן)
- מקור רשמי, **נפתח מחדש בפועל בסבב הביקורת** (code.claude.com/docs/en/memory, 2026-07-15):
  מאשר ש-CLAUDE.md הוא מנגנון רשמי ומתועד של Claude Code, נקרא בתחילת כל session, חלק ממערכת
  memory דו-שכבתית (קבצי CLAUDE.md + auto-memory), עם פקודות /init ו-/memory רשמיות. מצטט במפורש:
  "Claude treats them as context, not enforced configuration" ו-"there's no guarantee of strict
  compliance, especially for vague or conflicting instructions" — לפעולה שחייבת לקרות בכל פעם יש
  מנגנון נפרד (PreToolUse hook), לא CLAUDE.md עצמו.
- מקור רשמי, **נפתח מחדש בפועל בסבב הביקורת** (code.claude.com/docs/en/best-practices, 2026-07-15):
  תומך בעקרון "לא לבנות/לכתוב יותר ממה שצריך" (הנחיה מפורשת לגיזום קפדני של CLAUDE.md, קובץ ארוך
  מדי גורם ל-Claude "להתעלם מחצי ממנו"), ובעקרון בדיקת-תוצאה לפני הכרזת "גמור" ("Have Claude show
  evidence rather than asserting success").
- **תיקון ביקורת (חובה, ר' סעיף ביקורת בקובץ content-desk-package.md):** הטענה המספרית הקודמת
  ("נשמע אצלו ~70% מהפעמים, לא 100%", ממקור משני יחיד — iwoszapar.com) **הוסרה מהחבילה**. בפתיחה
  מחדש בפועל של שני המקורות הרשמיים, אף אחד מהם לא מציין אחוז ציות ספציפי; חיפוש טרי נוסף העלה
  מספרים שונים וסותרים במקורות משניים בלתי-רשמיים (70% / 80% / "60–70% ביום טוב") בלי עיגון
  במקור ראשוני אחיד — כלומר טענה מספרית לא-ניתנת-לאימות. הוחלפה בניסוח האיכותי המדויק מהמקור
  הרשמי עצמו ("הקשר, לא אכיפה - אין ערובה מלאה"), בכל מופעי הטענה בחבילה (תסריט, טקסט-על-מסך,
  caption).
- מספר מקורות טכניים עצמאיים (codersera.com, mcp.directory, marmelab.com, dev.to) תומכים בעיקרון
  הכללי: כללים מפורשים בקובץ הנחיות מפחיתים over-engineering; הפניה ל-specs בקובץ מפחיתה הזיות.
- ראיה פנימית (Used/Built): הפרויקט הנוכחי מכיל בפועל קובץ CLAUDE.md בשורש הריפו (מוצג בהדגמת
  המסך במלואו — סעיפי "מי אני", "ברירת מחדל עסקית", "מה אני בונה", "העדפות תקשורת", "מיתוג").
  שלושת הכללים המשמשים כהוכחת-מסך בשוטים 3–5 קיימים בפועל בסעיף "העדפות תקשורת" של הקובץ:
  נגד המצאת פרטים ("לא להמציא פרטים על מיתוג, קוד קיים, אינטגרציות, לקוחות, תמחור או סטטוס
  מוצר") וכנות אנליטית ("לא להסכים אוטומטית ולא לרצות אותי") היו קיימים מראש; הכלל נגד בנייה
  מוגזמת ("לא לבנות מערכת רחבה כשביקשתי שינוי ממוקד...") **נוסף לקובץ ב-2026-07-17** לבקשת שני,
  כדי שהטענה בריל תיתמך בהוכחת-מסך מדויקת מתוך הקובץ עצמו (ולא מסקיל/תוסף נפרד). קיים גם תוסף/
  סקיל פנימי נוסף (`karpathy-guidelines`) הפעיל בסביבת שני נגד over-engineering — **אינו** מוצג
  בשוט 3 בגרסה זו, כדי לא להציג קובץ/סקיל אחר כאילו הוא CLAUDE.md.
- "4 הבעיות" מהמחקר המקורי (בנייה מוגזמת, התעלמות מהוראות, טענת "סיימתי" מוטעית, הזיית API):
  העיקרון הכללי מאומת (ר' מקורות למעלה), אך התוכן המופק משתמש במכוון ב-**3 כללים בלבד** — אלה
  שיש להם הוכחת-מסך אמיתית ומדויקת בקובץ ה-CLAUDE.md האמיתי. לא נוסף כלל רביעי בלי ראיה אמיתית
  (הנחיית שני, סבב 3).
- זוהה והוחרג במכוון (חסום לציטוט, לא "לא ידוע"): שם/כתובת הריפו הספציפי מהרפרנס, זהות/handle
  היוצר המקורי, ופקודת ה-plugin/marketplace שהוצגה במסך ברפרנס — כל אלה אינם מוזכרים בשום מקום
  בחבילה זו (לא בתסריט, לא בכיתוב, לא ב-caption, לא בהערות הפקה).
- מה לא ידוע (בירושה, לא מטושטש): נתוני מעורבות מאומתים של הרפרנס (צפיות/לייקים/תאריך מדויק) —
  לא סופקו ולא אומתו; לכן אין בונוס "חדשות חמות <48 שעות" בציון. אין עדיין נתוני performance.md
  על תוכן חינוכי דומה אצל שני. כלי הקלטת המסך הקונקרטי (CAP-01 ב-`config/tools.md`) עדיין
  `unassigned` — דורש אישור שני לפני הפקה בפועל.

## תוכנית ויזואלית מפורטת (חבילת הפקה מורחבת — Inspiration-led)

**קונספט ליבה:** הוכחת מנגנון אמיתי ומתועד (קובץ הנחיות ל-Claude Code) דרך הדגמת מסך אמיתית של
הקובץ **האמיתי** של שני בפרויקט הזה — לא מוקאפ, לא הכלי/הריפו מהרפרנס.

**הוק סופי:** ר' "## הוק" למעלה.

**מבנה שוטים ממוספר (7 שוטים, 30 שניות — מבנה "בעיה קונקרטית → הוכחת מסך" מאומץ מהרפרנס, לא ניסוח.
אורך לא-אחיד לכל שוט, לפי תוכן; אין צורך לכפות 7 שוטים ל-10 אם 7 עובד טוב יותר — אישור שני):**

| # | זמן | סוג שוט | טקסט על המסך | מה מוקלט בפועל |
|---|---|---|---|---|
| 1 | 0–3ש' | הדגמת מסך אמיתית | "3 הרגלים שעצרתי עם קובץ אחד" | פתיחת הפרויקט, קובץ CLAUDE.md האמיתי בעורך — ההוק כטקסט על גבי ההקלטה עצמה |
| 2 | 3–6ש' | הדגמת מסך אמיתית | "CLAUDE.md = כללי העבודה של הפרויקט" | המשך אותה הקלטה — גלילה קלה לראש הקובץ, מציגה את שם הקובץ |
| 3 | 6–11ש' | הדגמת מסך אמיתית | "1. בלי בנייה מוגזמת" | גלילה בקובץ האמיתי לסעיף "העדפות תקשורת", התמקדות בשורה "לא לבנות מערכת רחבה כשביקשתי שינוי ממוקד..." (נוסף לקובץ ב-2026-07-17 לצורך הוכחת-מסך מדויקת — ר' הערה בסוף הטבלה) |
| 4 | 11–16ש' | הדגמת מסך אמיתית | "2. בלי להמציא מידע" | המשך אותו סעיף — התמקדות בשורה "לא להמציא פרטים על מיתוג, קוד קיים, אינטגרציות, לקוחות, תמחור או סטטוס מוצר" |
| 5 | 16–21ש' | הדגמת מסך אמיתית | "3. ביקורת אמיתית, לא ליטופים" | המשך אותו סעיף — התמקדות בשורה "כנות אנליטית. לא להסכים אוטומטית ולא לרצות אותי..." |
| 6 | 21–25ש' | הדגמת מסך אמיתית | "קובץ אחד. הקשר קבוע." | סגירת חלון העורך / זום-אאוט כללי על הקובץ הפתוח — משפט סיכום, לא הדגמה חדשה |
| 7 | 25–30ש' | CTA+Outro (גרפיקה ממותגת, לא הקלטת מסך) | ראשי: "כתבו 'הנחיות'" · משני: "לקבלת רשימת הכללים המלאה" · תחתית: @shani.creates.ai / SHANI-AI.COM | גרפיקה ממותגת יחידה — משלבת CTA וסיום, בלי שוט Outro נפרד |

> **הערה טכנית לשוטים 3–5 (חובה לקרוא לפני הקלטה):** כל שלושת השוטים מציגים גלילה באותו קובץ
> CLAUDE.md האמיתי, בתוך סעיף "העדפות תקשורת" — לא בקובץ/סקיל נפרד. שורת "לא לבנות מערכת רחבה..."
> (שוט 3) **נוספה בפועל לקובץ ב-2026-07-17**, לבקשת שני, במיוחד כדי שהטענה בריל תהיה מדויקת מול
> הוכחת-מסך אמיתית (ולא תציג סקיל נפרד כאילו הוא חלק מ-CLAUDE.md). שורות "לא להמציא פרטים..."
> (שוט 4) ו-"כנות אנליטית..." (שוט 5) היו קיימות בקובץ מראש, ללא שינוי.

**חלופת טקסט-בלבד (בלי קריינות):** שוטי הכיתוב-על-מסך (עמודה השנייה בטבלה) בנויים לעמוד לבד —
אם לא מוקלטת קריינות בפועל, מסתמכים על הכיתוב + מוזיקת רקע בלבד. זו ברירת המחדל הבטוחה אם
הקלטת קול לא ודאית, ותלויה גם בהחלטת שני (ר' "מה נשאר לשני" למטה).

**Checklist הקלטת מסך (חובה — אמיתי, לא מוקאפ):**
- מסך פתוח: חלון עורך/טרמינל נקי שמציג את קובץ ה-CLAUDE.md האמיתי של הפרויקט הזה — בלי טאבים/
  קבצים לא קשורים גלויים.
- נקודת התחלה מדויקת: הרגע שבו קובץ ה-CLAUDE.md נפתח במלואו בעורך — ההקלטה מתחילה משם (שוט 1;
  ההוק מונח כטקסט על גבי ההקלטה עצמה, לא על גרפיקה נפרדת).
- להראות: הקובץ פתוח → גלילה קלה לראש → גלילה וזום על סעיף "העדפות תקשורת" (3 השורות: נגד
  בנייה מוגזמת, נגד המצאת מידע, כנות אנליטית) → סגירה/זום-אאוט (שוטים 1–6).
- נקודת סיום מדויקת: סגירת חלון העורך/המסך אחרי שוט 6 — לעצור שם, לפני כל תוכן לא קשור. שוט 7
  אינו הקלטת מסך — גרפיקה ממותגת בלבד.
- **חובה להסתיר/לטשטש:** נתיב קובץ מקומי מלא, שם משתמש Windows, כל מפתח API/טוקן, שמות
  לקוחות/פרויקטים לא-קשורים, כל טאב דפדפן/סימניה לא רלוונטיים. **בדיקה כפולה חובה:** לוודא שאין
  בהקלטה שום רמז לשם/כתובת הריפו מהרפרנס, לזהות היוצר המקורי, או לפקודת ה-plugin/marketplace
  שהוצגה שם — לא בטאב פתוח, לא בהיסטוריה, לא בפלט מקרי (ר' `config/tools.md` CAP-01/CAP-02
  לגבי איסור מוקאפ AI על ממשק Claude אמיתי — אותו כלל חל כאן על תוכן חסום לציטוף).
- **תלות פתוחה חובה למפיקה:** כלי הקלטת המסך הקונקרטי (CAP-01 ב-`config/tools.md`) עדיין
  `unassigned` — לא ניתן להקליט את שוטים 1–6 בפועל עד לאישור שני לכלי. אין להחליף במוקאפ AI.

**גרפיקות ממותגות נדרשות:** כרטיס CTA+Outro משולב יחיד לשוט 7 (רקע/צבעים/פונטים לפי
`brand/visual.md` — לא נקרא ע"י הכותבת, לפתוח לפני עיצוב), שכבת כיתוב מסונכרנת לשוטים 1–6
(overlay על ההקלטה האמיתית, לא מחליפה אותה).

**מעברים וקצב:** חיתוכים קשים (hard cuts) בין שוטים, בלי קרוספייד — תואם את קצב הרפרנס (מבנה, לא
ניסוח). זום קל אפשרי בשוטים 3–5 להדגשת השורה הספציפית בקובץ לכל כלל.

**כיוון סאונד:** לא נקבע כאן טרק ספציפי — אין מידע על זמינות מוזיקה/רישיון בפועל. אם מוקלטת
קריינות אמיתית — היא פס הקול הראשי, מוזיקת רקע שקטה מתחתיה. אם לא — כתוביות + מוזיקת רקע בלבד
(מהספרייה הזמינה באינסטגרם בזמן ההעלאה בפועל).

**Story המשך (נפרד מה-CTA של הריל):** מדבקת שאלה בסטורי לאחר הפרסום: "מה הייתם רוצים שיהיה
בקובץ הנחיות כזה?" — מוביל לתגובות/DM, לא CTA נוסף בגוף הריל עצמו (חוק ה-CTA היחיד שמור).

**הערת handoff ל-ManyChat (טיוטה בלבד):** מילת המפתח "הנחיות" מוצעת ל-CTA "כתבו מילה ואשלח",
טרם שוריינה ב-`keywords.md`, וטרם מחוברת לאוטומציה בפועל ב-ManyChat — מחוץ לתחום המנוע, באחריות
שני.

**רשימה מפורשת — מה נשאר לשני לבצע בפועל:**
1. לאשר כלי הקלטת מסך בפועל (CAP-01 עדיין `unassigned`) — בלי זה אי אפשר להקליט שוטים 1–6.
2. להחליט: קריינות מוקלטת בפועל, או חלופת הטקסט-בלבד.
3. לפתוח את `brand/visual.md` לפני בניית כרטיס ה-CTA+Outro (שוט 7).
4. לאשר/לשריין את מילת המפתח "הנחיות" ב-`keywords.md` לפני פרסום, או לבחור מילה חלופית.
5. לאשר את קונספט המדריך לפני הפקה בפועל דרך `content/gen_guide.py`.
6. לבדוק ידנית שאין בהקלטה בפועל שום רמז לריפו/יוצר/פקודת plugin מהרפרנס, נתיב מקומי, שם
   משתמש או מפתח.
7. להעביר למבקרת (reviewer) לפני פרסום — לפי תהליך Content Desk הרגיל. אין פרסום/תזמון/שליחה
   בפועל כחלק מהמשימה הזו.

## הערות הפקה (למפיקה)
- פורמט טכני: 1080×1920 (9:16), אורך כולל 30 שניות, 7 שוטים — פירוט מלא בטבלה למעלה.
- הדגמת המסך (שוטים 1–6) חייבת להיות צילום אמיתי של הקובץ האמיתי — לא מוקאפ AI, לא הכלי/הריפו
  מהרפרנס (ר' `config/tools.md` CAP-01/CAP-02). שוט 7 הוא גרפיקה ממותגת בלבד, לא הקלטת מסך.
- כיוון עיצוב: `brand/visual.md` — לא נקרא בשלב זה, לפתוח לפני בניית גרפיקות/כיתובים.
- כל טענה עובדתית בטקסט השיווקי מעוגנת בסעיף "מקורות ועובדות" למעלה — אין טענה שאין לה עיגון שם.
- קונספט המדריך מיועד כקלט טקסטואלי בלבד ל-`content/gen_guide.py` — לא קובץ מוגמר.
- תלות פתוחה קריטית: CAP-01 (כלי הקלטת מסך) `unassigned` — לא להתחיל הפקה בפועל של שוטים 1–6
  לפני אישור שני.
- **פנייה ניטרלית-מגדרית (הנחיית שני, סבב 3):** אין להשתמש ב"רוצות", "בעלות עסקים" או פנייה
  נשית-בלעדית אחרת בטקסט השיווקי הישיר (תסריט/כיתוב/caption/CTA) — הפנייה חייבת להתאים לכל
  המגדרים. גוף ראשון ("אני"/"כתבתי"/"ביקשתי") נשאר כפי שהוא — צורות אלה ניטרליות מגדרית בעברית.
- חוקי פרטיות: לעולם לא לכתוב/להציג שם משתמש Windows, נתיב מקומי מלא, מפתחות/טוקנים, שמות
  לקוחות/פרויקטים לא-קשורים בשום קובץ או פריים.
- איסור מוחלט: שם/כתובת הריפו הספציפי מהרפרנס, זהות/handle היוצר המקורי, ופקודת ה-plugin/
  marketplace שהוצגה במסך ברפרנס — לא מוזכרים בשום מקום (וידאו/כיתוב/תיאור/תגובה), גם לא
  בעקיפין.

## לוג ben-adam
סקיל ben-adam לא היה זמין ככלי בסביבת הריצה הזו (אין הרשאת/גישת כלי מתאימה לסוכנת בסשן הנוכחי)
— **לא זמין בריצה זו**, לא הורץ בפועל. במקומו בוצעה בדיקה ידנית של כל הטקסטים השיווקיים (הוק,
תסריט/קריינות, טקסט-על-מסך, caption) מול רשימת האיסורים ב-`brand/voice.md`: אין עברית-של-AI
("יתרה מזאת" וכד'), אין ניפוח ("מהפכני"/"משנה חיים"), אין אימוג'ים בקריינות/תסריט/caption (אימוג'י
📌 שהיה ב-caption בגרסה הקודמת הוסר בסבב 3), אין משפט מועתק מהרפרנס (התוכן מקורי לחלוטין), משפטים
קצרים בגוף ראשון (ניטרלי מגדרית), CTA יחיד. **סבב 3 (תיקון פנייה ניטרלית-מגדרית):** נבדק ישירות
מול הטקסט הסופי — אין "רוצות", "בעלות עסקים" או "לכן" בשום מקום בתסריט/טקסט-על-מסך/caption/CTA.
זו אינה תחליף להרצת הסקיל בפועל אם/כשיהיה זמין — יש לסמן זאת למבקרת.

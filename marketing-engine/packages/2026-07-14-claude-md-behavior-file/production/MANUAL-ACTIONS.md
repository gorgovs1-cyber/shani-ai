# MANUAL-ACTIONS.md — מה נשאר לשני

> Review status: `approved` (**סבב 3**, 2026-07-17). Shani status: `pending`. Operational readiness: `blocked`.
> הפקה זו לא מפרסמת, לא מתזמנת ולא שולחת דבר.

## מה תוקן בסבב 3 (ביוזמת שני, 2026-07-17)
תוכן החבילה תוקן במלואו כדי לחזור למבנה הרפרנס האמיתי ("בעיה קונקרטית → הוכחת מסך אמיתית"):
7 שוטים / 30 שניות במקום 10/35, שלושה כללים קונקרטיים ומוכחים ישירות מתוך `CLAUDE.md` האמיתי
(כולל כלל חדש נגד בנייה מוגזמת שנוסף לקובץ במיוחד לשם כך), פנייה ניטרלית-מגדרית מלאה בכל הטקסט
השיווקי, וכרטיס CTA+Outro משולב יחיד בשוט 7 במקום שוט Outro נפרד. פירוט מלא ב-
`content-desk-package.md` סעיף 5ב, כולל Evidence block של ביקורת ה-Reviewer העצמאית (`approved`).
כל קבצי ה-production תחת תיקייה זו נדרסו ועודכנו בהתאם — הגרסה הקודמת (10 שוטים/35ש', סבב 2)
שמורה בהיסטוריית git בלבד.

## מה בוצע בהפקה הזו
1. כל קבצי ה-production/ (final-script, caption, voiceover, on-screen-text, subtitles, shot-list,
   screen-recording-checklist) נדרסו ועודכנו כלשונם מ-`package.md`/`content-desk-package.md`
   הגרסה המאושרת (סבב 3, 7 שוטים).
2. רינדור ויזואלי אוטומטי הורץ מחדש (`render_visuals.py --type reel`) מול מבנה `REEL_PACKAGES`
   המעודכן של החבילה הזו בלבד: קאבר + כרטיס CTA+Outro יחיד (שוט 7) + 6 overlays שקופים לשוטים
   1–6 + 6 סלוטי הקלטה + contact sheet. אין שינוי לכלי המשותף עצמו מעבר לנתוני החבילה הספציפית
   ותמיכת `subtext` (טקסט משני קטן) שכבר הייתה קיימת בפונקציות הרינדור אך לא הייתה מחוברת.
3. שום חבילה אחרת לא נגעה — רק `2026-07-14-claude-md-behavior-file/production/`.

## תלויות פתוחות — לביצוע שני
1. **לאשר כלי הקלטת מסך (CAP-01)** ב-`config/tools.md` (עדיין `unassigned`), ואז להקליט בפועל
   את שוטים 1–6 לפי `screen-recording-checklist.md` — הקלטה אמיתית של קובץ ה-CLAUDE.md האמיתי,
   לא מוקאפ. שוט 7 אינו הקלטת מסך.
2. **להחליט על קריינות:** להקליט קול אמיתי לפי `voiceover.txt`, או לבחור בחלופת הטקסט-בלבד
   (הכיתובים ב-`on-screen-text.csv` + מוזיקת רקע, בלי קריינות).
3. **להרכיב בפועל** ב-CapCut (או כלי מקביל) באמצעות `subtitles.srt`, כרטיס ה-CTA+Outro המלא
   ב-`visuals/png/cta-shot7.png` (מוכן לשימוש ישיר) וה-overlays השקופים ב-`visuals/overlays/`
   (הנחה מעל הקלטת שוטים 1–6 בפועל, לא במקומה). ר' `CONTACT-SHEET.png` לתצוגה כוללת, ו-
   `visual-timeline.csv`/`visual-manifest.json` למיפוי מדויק לפי זמן.
4. **לאשר/לשריין את מילת המפתח "הנחיות"** ב-`keywords.md` לפני פרסום, או לבחור מילה חלופית —
   טרם מחוברת לאוטומציה ב-ManyChat.
5. **לאשר את קונספט המדריך** (סעיף 4 ב-`content-desk-package.md`) לפני הפקה בפועל דרך
   `content/gen_guide.py` — לא נוצר PDF, לא פורסם, אין קישור פעיל.
6. **חסם תפעולי:** אסור לפרסם לפני ששלושת הפריטים (הקלטה, מילת מפתח משוריינת, PDF המדריך/עמוד
   באתר) קיימים בפועל — או, לחלופין, לבחור לפרסם עם CTA לשמירת הפוסט (save-CTA) במקום מילת
   מפתח, לפי שיקול דעתך.
7. **בדיקה ידנית לפני הרכבה סופית:** לוודא שאין בהקלטה בפועל שום רמז לריפו/יוצר (nathanhodgson.ai)
   /פקודת plugin מהרפרנס, נתיב מקומי, שם משתמש או מפתח — לפי `screen-recording-checklist.md`.
8. **אישור סופי = Shani status.** רק שני קובעת מתי החבילה עוברת מ-`pending` הלאה.

# MANUAL-ACTIONS.md — מה נשאר לשני

> Review status: `approved`. Shani status: `pending`. Operational readiness: `blocked`.
> הפקה זו לא מפרסמת, לא מתזמנת ולא שולחת דבר.

1. **לאשר כלי הקלטת מסך (CAP-01)** ב-`config/tools.md` (עדיין `unassigned`), ואז להקליט
   בפועל את שוטים 1–7 לפי `screen-recording-checklist.md` — הקלטה אמיתית של התהליך, לא מוקאפ.

2. **להחליט על קריינות:** להקליט קול אמיתי לפי `voiceover.txt`, או לבחור בחלופת הטקסט-בלבד
   (הכיתובים ב-`on-screen-text.csv` + מוזיקת רקע, בלי קריינות).

3. **להרכיב בפועל ב-CapCut** (או כלי עריכה מקביל) באמצעות `subtitles.srt`, הכרטיסים המלאים
   ב-`visuals/png/` (`cover.png`, `cta-shot8.png`, `cta-shot9.png` — מוכנים לשימוש ישיר),
   ה-overlays השקופים ב-`visuals/overlays/` (הנחה מעל הקלטת שוטים 1–7 בפועל, לא במקומה) ותגי
   "שלב 1–4" האופציונליים (`step-badge-4..7`) — יחד עם הקלטת שוטים 1–7 עצמה. ר' `CONTACT-SHEET.png`
   לתצוגה כוללת של הרצף, ו-`visual-timeline.csv`/`visual-manifest.json` למיפוי מדויק לפי זמן.

4. **חסם תפעולי — חשוב:** אסור לפרסם לפני שנתיב המשלוח של "תחקיר" קיים בפועל (PDF המדריך +
   עמוד באתר + שריון מילת המפתח ב-ManyChat) — או, לחלופין, לבחור לפרסם עם CTA לשמירת הפוסט
   (save-CTA) במקום מילת מפתח, לפי שיקול דעתך.

5. **אישור סופי = Shani status.** רק שני קובעת מתי החבילה עוברת מ-`pending` הלאה.
